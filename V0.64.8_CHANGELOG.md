# v0.64.8

- Fixed `/orders` returning `DB_SCHEMA_OUTDATED` when `gift_offers.seller_profile_id` had not been installed.
- Added backward-compatible incoming-offer query through the existing Gift foreign key.
- Realtime automatically avoids the unavailable seller filter while the database is in compatibility mode.
- Added safe, idempotent Orders runtime migration `100003_orders_runtime_compat_v0648.sql`.
