# MemeX Market v0.64.1

## Build hotfix

- Fixed `TS2353` in `app/cases/page.tsx` caused by TypeScript preserving the narrower inferred element type returned by `Array.from`.
- The case roulette collection is now explicitly typed as `ReelItem[]`, so the server-selected winner marker `final: true` is type-safe.
- No database migration changes are required over v0.64.0.
